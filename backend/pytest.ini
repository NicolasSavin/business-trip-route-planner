[pytest]
markers =
    unit: mandatory tests that must pass before merge.
    integration: tests that require integration infrastructure or external services.
    live: tests that call live external services.
